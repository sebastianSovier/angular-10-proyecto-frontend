import { ListaBeneficiariosModel } from './lista-beneficiarios-model.model';

describe('ListaBeneficiariosModel', () => {
  it('should create an instance', () => {
    expect(new ListaBeneficiariosModel()).toBeTruthy();
  });
});
