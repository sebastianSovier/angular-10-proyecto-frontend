import { ObtienePeriodoActual } from './obtiene-periodo-actual.model';

describe('ObtienePeriodoActual', () => {
  it('should create an instance', () => {
    expect(new ObtienePeriodoActual()).toBeTruthy();
  });
});
