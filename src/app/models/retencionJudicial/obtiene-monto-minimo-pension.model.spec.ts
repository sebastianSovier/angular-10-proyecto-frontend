import { ObtieneMontoMinimoPension } from './obtiene-monto-minimo-pension.model';

describe('ObtieneMontoMinimoPension', () => {
  it('should create an instance', () => {
    expect(new ObtieneMontoMinimoPension()).toBeTruthy();
  });
});
