import { ObtieneListaBancos } from './obtiene-lista-bancos.model';

describe('ObtieneListaBancos', () => {
  it('should create an instance', () => {
    expect(new ObtieneListaBancos()).toBeTruthy();
  });
});
