import { ObtieneListaFormaPagos } from './obtiene-lista-forma-pagos.model';

describe('ObtieneListaFormaPagos', () => {
  it('should create an instance', () => {
    expect(new ObtieneListaFormaPagos()).toBeTruthy();
  });
});
